{
  "$id": "http://github.com/tapis-project/authenticator/configschema.json",
  "$schema": "http://json-schema.org/schema#",
  "properties": {
    "sql_db_url": {
      "type": "string",
      "description": "full URL, including protocol and database, to the SQL db.",
      "default": "postgres://authenticator:d3fd6pAssw0rd@postgres:5432/authenticator"
    }
  }
}